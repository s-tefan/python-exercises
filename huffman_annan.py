class MyTree:
    def MyTree(data = None):
        left_child = None
        right_child = None
        data = None

class MyHoffman(MyTree):
    def encode_frequency_table(td):
        """
        Encode a table td, as a dict with any objects as keys and is frequencies 
        as values to a Hoffman tree as a MyHoffman object
        """


    @staticmethod
    def put_it_in(tree_list):
        if tree_list:
            pass
        else:
            tree_list

    @class
    def sorted_list(cls, td):
        sl = []
        for i in td.items():
            if 

